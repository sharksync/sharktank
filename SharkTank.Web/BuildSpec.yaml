version: 0.2

phases:
  build:
    commands:
      - apt-get install -y nodejs
      - node --version
      - dotnet restore
      - dotnet build --configuration release
      - dotnet publish --configuration release
artifacts:
  files:
    - wwwroot/*